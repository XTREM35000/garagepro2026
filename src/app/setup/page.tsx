import React from "react";
import SetupRoot from "./setup-root";

export const metadata = {
  title: "Initial Setup - SaaS Manager",
};

export default function SetupPage() {
  return <SetupRoot />;
}
